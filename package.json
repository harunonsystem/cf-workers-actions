{
  "name": "cf-workers-actions",
  "version": "1.3.2",
  "private": true,
  "description": "GitHub Actions for Cloudflare Workers deployment, PR commenting, and cleanup",
  "keywords": [
    "cloudflare",
    "deployment",
    "github-actions",
    "preview",
    "workers"
  ],
  "license": "MIT",
  "author": "harunonsystem",
  "main": "index.js",
  "scripts": {
    "build": "pnpm run clean:dist && node scripts/build.mjs",
    "build:watch": "tsc --watch",
    "clean:dist": "rm -rf dist",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run typecheck && pnpm run format:check && pnpm run lint && pnpm test",
    "prepare": "pnpm run build",
    "version": "pnpm run build && git add dist/"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.1.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/node": "^26.4.1",
    "@vercel/ncc": "^0.45.0",
    "@vitest/coverage-v8": "^5.0.0",
    "@vitest/ui": "^5.0.0",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vitest": "^5.0.0",
    "yaml": "^2.8.3"
  },
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=11.22.0"
  },
  "packageManager": "pnpm@11.22.0"
}
